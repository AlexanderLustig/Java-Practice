/* day 29 (8/20/2025) -- Boring shitty day today. Jenan has left me on seen since Sunday(Wednesday today), Mom called me boring and that my hobbies of anime and nerdy shit 
is boring and that im a loner and no girl gonna wan to go out with me cause of that(which kinda is true ngl). And also the weeather is shit today. But I did do some work on my
discord bot today and I got my John Jay ID yesterday. Also changed out my schedule so now i dont have a gap so no subway. WHich idk like ya im saving money but like that would
have been like a nice way to treat myself but now nope. Also if Jenan keeps acting like this imma be be done like I cant anymore its just not that enjoyable for me to deal with
the girl ur supposidly dating to not want to talk or hangout with you. Idk im just lonely man like why tf do I talk to myself more then other people. Idk I just want
Helldivers 2 to release sooner so maybe I can distract myself with that. Im just so boredd and its so quiet rn and I dont like it. Idk it kinda does suck that my interests leaned
towards nerdy shit cause Mom was right no girl or even anyone wants to really talk to me. Like it sucks casue even nerdy guys dont wanna really hangout cause idk actually. It just sucks tbh. 

*/

// day 29 (8/20/2025) -- 

import java.util.Scanner;

public class Main29 {
    static Scanner scanner = new Scanner(System.in);
    static double balance = 0.0;

    public static void main(String[] args) {

        int choice = mainLoop();

        while (choice != 4) {
            if (choice == 1) {
                balance();
            } else if (choice == 2) {
                deposit();
            } else if (choice == 3) {
                withdraw();
            } else {
                System.out.println("Invalid choice.");
            }

            System.out.println("Anything else you wish to do today?");
            choice = mainLoop();
        }

        System.out.println("Have a good day");
        scanner.close();
    }

    static int mainLoop() {
        System.out.println("1. Show Balance");
        System.out.println("2. Deposit");
        System.out.println("3. Withdraw");
        System.out.println("4. Exit");
        System.out.print("Enter your choice (1-4): ");
        int choice = scanner.nextInt();
        return choice;
    }

    static void balance() {
        System.out.println("Your current balance is : $" + balance);
    }

    static void deposit() {
        System.out.print("How much are you depositing? : ");
        double depositAmount = scanner.nextDouble();

        balance = balance + depositAmount;

        System.out.println("You just deposited: $" + depositAmount);
        System.out.println("Your current balance is now: $" + balance);
    }

    static void withdraw() {
        System.out.print("How much are you withdrawing? : ");
        double withdrawAmount = scanner.nextDouble();

        if (withdrawAmount > balance) {
            System.out.println("Insufficient funds.");
        } else {
            balance = balance - withdrawAmount;
            System.out.println("You just withdrew: $" + withdrawAmount);
            System.out.println("Your current balance is now: $" + balance);
        }
    }
}

